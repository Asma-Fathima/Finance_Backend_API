# Finance_Backend_API
Finance Data Processing and Access Control Backend is a RESTful API built using Python (Flask) and SQLite that simulates the backend of a finance dashboard system. The application enables different types of users to manage financial records based on their assigned roles and permissions.
